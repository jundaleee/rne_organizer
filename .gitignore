# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.ipynb_checkpoints/

# 비밀값 - Claude API 키 등은 절대 커밋 금지
.env
.streamlit/secrets.toml
.local_secrets.json

# 학습된 모델 파일 - scripts/build_final_model.py로 언제든 재생성 가능하므로 제외
# (재현성은 pkl을 보관하는 게 아니라, pkl을 만든 스크립트+데이터+커밋해시를 보관하는 것으로 확보한다)
models/*.pkl
!models/.gitkeep

# OS / 에디터
.DS_Store
.vscode/
*.swp
